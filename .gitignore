__pycache__/
*.pyc
.venv/
.env
.pytest_cache/

# Runtime data is generated by the workflow on first run; each deployment
# (fork) accumulates its own state/history, so it shouldn't ship in the template.
data/
